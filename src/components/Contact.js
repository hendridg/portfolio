import React from 'react';
import Button from './Button';

const Contact = () => (
  <div>
    <h2>
      I&#39;m always interested in hearing about new projects, so if you&#39;d
      like to chat please get in touch.
    </h2>
    <form>
      <input />
      <input />
      <textarea />
      <Button accent>Get in touch</Button>
    </form>
  </div>
);

export default Contact;
